{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*"
    ]
  },
  "scripts": {
    "build": "yarn workspace api build",
    "start": "lerna run start",
    "build-front": "yarn workspace frontend build",
    "dev": "lerna run dev"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "engines": {
    "node": "14.x"
  }
}
